# TrackStockz Server Environment Configuration

# --- Server Settings ---
# Port for the server to listen on (default 8000).
PORT=8000
# Node environment mode (development/production).
NODE_ENV=development

# --- CORS & Integration ---
# URL of the frontend application (for redirection and CORS).
FRONTEND_URL=http://localhost:5173
# Allowed Origin for Cross-Origin Resource Sharing.
CORS_ORIGIN=http://localhost:5173

# --- Database ---
# Connection string for MongoDB Atlas.
MONGODB_URI="mongodb+srv://<username>:<password>@cluster0.example.mongodb.net"

# --- Authentication (JWT) ---
# Secret keys for signing tokens. Use strong, random strings.
ACCESS_TOKEN_SECRET="YOUR_ACCESS_TOKEN_SECRET"
ACCESS_TOKEN_EXPIRY=1d

REFRESH_TOKEN_SECRET="YOUR_REFRESH_TOKEN_SECRET"
REFRESH_TOKEN_EXPIRY=10d

# --- Google OAuth (Social Login) ---
# Credentials from Google Cloud Console.
GOOGLE_CLIENT_ID="YOUR_GOOGLE_CLIENT_ID"
GOOGLE_CLIENT_SECRET="YOUR_GOOGLE_CLIENT_SECRET"
# Refresh token for offline access to Google APIs (e.g. Gmail).
GOOGLE_REFRESH_TOKEN="YOUR_GOOGLE_REFRESH_TOKEN"

# --- Cloudinary (Image Storage) ---
# Credentials from Cloudinary Dashboard.
CLOUDINARY_CLOUD_NAME="YOUR_CLOUD_NAME"
CLOUDINARY_API_KEY="YOUR_API_KEY"
CLOUDINARY_API_SECRET_KEY="YOUR_API_SECRET_KEY"

# --- Email Service (Nodemailer) ---
# Gmail address used for sending system emails.
EMAIL_USER="your_email@gmail.com"
# App Password generated from Google Account Security settings (NOT your login password).
APP_PASS="your_app_password"
